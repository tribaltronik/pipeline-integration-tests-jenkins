FROM jenkins/jenkins:jdk21

USER root

# Install Maven and Docker CLI
RUN apt-get update && apt-get install -y maven docker.io && rm -rf /var/lib/apt/lists/*

# Add jenkins user to docker group
RUN usermod -aG docker jenkins

USER jenkins

# Skip setup wizard
ENV JAVA_OPTS="-Djenkins.install.runSetupWizard=false"

# Install plugins via jenkins-plugin-cli
RUN jenkins-plugin-cli --plugins \
    workflow-aggregator \
    git \
    pipeline-stage-view \
    junit \
    docker-workflow \
    configuration-as-code \
    pipeline-utility-steps \
    timestamper

# Disable CSRF by default
RUN echo 'jenkins.model.Jenkins.instance.setCrumbIssuer(null)' >> /usr/share/jenkins/ref/init.groovy.d/disable-csrf.groovy
